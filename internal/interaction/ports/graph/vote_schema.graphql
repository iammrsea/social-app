type Vote {
  userId: String!
  postId: String!
  type: String!
}

type Query {
  getVotes: [Vote]!
}

input VoteInput {
  userId: String!
  postId: String!
  type: String!
}

type Mutation {
  vote(input: VoteInput): Vote
}
